const authRoutes = {
  login: {
    path: "/auth/login",
  },
  register: {
    path: "/auth/register",
  },
};

const rootRoutes = {
  home: {
    path: "/",
  },
};

export { authRoutes, rootRoutes };
